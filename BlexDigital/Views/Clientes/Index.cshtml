@model IEnumerable<Sistema_de_gestion_comercial_Blex_Digital.Models.Cliente>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NombreCliente)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CorreoDeContacto)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CelularDeContacto)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NombreCliente)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CorreoDeContacto)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CelularDeContacto)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Dni }) |
            @Html.ActionLink("Details", "Details", new { id=item.Dni }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Dni })
        </td>
    </tr>
}

</table>
